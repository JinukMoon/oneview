// Browser stub for Node's `fs` (pulled in transitively by cfb inside hwp.js).
// hwp.js never hits the filesystem in the browser path, so an empty object is safe.
export default {};
export const readFileSync = undefined;
export const writeFileSync = undefined;
